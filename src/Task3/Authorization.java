package Task3;

public class Authorization extends Авторизація implements AuthorizationInterface {
    @Override
    public boolean authorize(DataBase dataBase) {
        return super.авторизуватися(dataBase);
    }
}
